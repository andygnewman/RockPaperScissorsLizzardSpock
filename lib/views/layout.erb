<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>r:p:s</title>
    <link rel="stylesheet" href="normalize.css">
    <link rel="stylesheet" href="andy_style.css">
  </head>
  <body>
    <section id="container">
      <header><img class="click_image" height="180" width="180"
        alt="rock, paper, scissors, lizard, spock"
        src="images/Pierreciseauxfeuillelizzardspockaligned.svg">
        <h2 class="slider_title"><%= GAME.possible_values_display %>
          <br>..just because you may be bored</h2>
      </header>

      <a href="/new_game">Reset Game</a><br>

      <p class="result"><%= "#{@p1_name} chose #{@p1_choice}, #{@p2_name}
        chose #{@p2_choice}" if @p1_choice != nil %></p>
      <p class="result"><%= @result %></p>

      <p class="scores"><%= "Current Scores: #{@p1_name}: #{@p1_score}
        #{@p2_name}: #{@p2_score}" %></p>

      <p class="error"><%= @error %></p>

        <%= yield %>

      <footer>
        <small>2015 Andy Newman</small>
      </footer>
    </section>
  </body>
</html>
